# Telegram-бот-планировщик для Google Calendar

Учебный проект Telegram-бота, который принимает естественные фразы на русском языке, распознает намерения через LLM (Open Router / Mistral) и управляет событиями в Google Calendar.

## Возможности

- ✅ **Создание событий** - просто скажите боту, например: "назначь встречу на завтра в 15:00"
- 📅 **Просмотр событий** - запросите список событий на конкретную дату
- ❌ **Удаление событий** - удаляйте события по названию или ID
- 🤖 **Естественный язык** - бот понимает фразы на русском языке
- 🔍 **Уточнения** - если данных недостаточно, бот задаст уточняющие вопросы

## Технологии

- Python 3.10+
- python-telegram-bot (v20+)
- Google Calendar API
- Open Router API (Mistral)
- Pydantic для валидации
- python-dotenv для конфигурации

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd Bot_Test
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
```

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте `.env.example` в `.env`:

```bash
copy .env.example .env  # Windows
# или
cp .env.example .env    # Linux/Mac
```

Отредактируйте `.env` и заполните необходимые значения:

```env
TELEGRAM_TOKEN=your_telegram_token_here
OPENROUTER_API_KEY=your_openrouter_api_key_here
GOOGLE_CREDENTIALS_PATH=./credentials.json
GOOGLE_TOKEN_PATH=./token.json
TIMEZONE=Europe/Moscow
DEFAULT_EVENT_DURATION_MIN=60
LLM_CONFIDENCE_THRESHOLD=0.80
```

### 5. Получение Telegram Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен и вставьте в `.env` как `TELEGRAM_TOKEN`

### 6. Получение Open Router API Key

1. Зарегистрируйтесь на [Open Router](https://openrouter.ai/)
2. Перейдите в раздел API Keys
3. Создайте новый API ключ
4. Скопируйте ключ и вставьте в `.env` как `OPENROUTER_API_KEY`

### 7. Настройка Google Calendar API

#### Шаг 1: Создание проекта в Google Cloud Console

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google Calendar API:
   - Перейдите в "APIs & Services" > "Library"
   - Найдите "Google Calendar API"
   - Нажмите "Enable"

#### Шаг 2: Создание OAuth 2.0 Credentials

1. Перейдите в "APIs & Services" > "Credentials"
2. Нажмите "Create Credentials" > "OAuth client ID"
3. Выберите "Desktop app" в качестве типа приложения
4. Назовите клиент (например, "Telegram Calendar Bot")
5. Нажмите "Create"
6. Скачайте JSON файл с учетными данными
7. Переименуйте файл в `credentials.json` и поместите его в корневую директорию проекта

**Важно:** Файл `credentials.json` содержит секретные данные и не должен попадать в репозиторий!

#### Шаг 3: Первый запуск и авторизация

При первом запуске бота:

1. Бот откроет браузер для авторизации Google
2. Войдите в свой Google аккаунт
3. Предоставьте разрешения для доступа к календарю
4. После успешной авторизации токен будет сохранен в `token.json`
5. При последующих запусках авторизация не потребуется (пока токен не истечет)

## Запуск

### Простой запуск

```bash
python main.py
```

### Использование скрипта (Linux/Mac)

```bash
chmod +x run_local.sh
./run_local.sh
```

### Использование скрипта (Windows)

```powershell
.\run_local.bat
```

После запуска вы увидите сообщение:
```
✅ Бот запущен! Нажмите Ctrl+C для остановки.
```

Откройте Telegram и начните общение с вашим ботом командой `/start`.

## Использование

### Команды бота

- `/start` - приветствие и инструкция по использованию
- `/add <текст>` - альтернативный способ создания события
  - Пример: `/add назначь встречу на завтра в 15:00`
- `/view YYYY-MM-DD` - просмотр событий на дату
  - Пример: `/view 2025-11-27`
- `/delete <event_id|название>` - удаление события
  - Пример: `/delete abc123xyz` или `/delete встреча с Вадимом`

### Примеры естественных фраз

**Создание события:**
- "назначь на завтра встречу в 15:00 с вадимом"
- "создай событие совещание на понедельник в 10 утра в офисе"
- "добавь встречу с коллегами на послезавтра в 14:30 в zoom"
- "создай событие презентация проекта на 30 ноября в 16:00 длительностью 2 часа"

**Просмотр событий:**
- "покажи события на 27 ноября"
- "какие события у меня сегодня"
- "что у меня запланировано на завтра"

**Удаление события:**
- "удали встречу с вадимом завтра"
- "удали событие abc123xyz"

### Примеры диалогов

#### Пример 1: Создание события

```
Вы: назначь на завтра встречу в 15:00 с вадимом

Бот: ✅ Событие создано!

📅 Встреча с Вадимом
🕐 27.11.2025 15:00
👥 Участники: Вадим

ID события: abc123xyz...
🔗 https://calendar.google.com/calendar/event?eid=...
```

#### Пример 2: Просмотр событий

```
Вы: покажи события на 27 ноября

Бот: 📅 События на 27.11.2025:

1. 🕐 10:00 - Совещание команды
   ID: def456abc...

2. 🕐 15:00 - Встреча с Вадимом
   ID: abc123xyz...
```

#### Пример 3: Удаление с подтверждением

```
Вы: удали встречу с вадимом завтра

Бот: Найдено событие:

📅 Встреча с Вадимом
🕐 2025-11-27 15:00

Удалить это событие? (Да / Нет)

Вы: Да

Бот: ✅ Событие удалено.
```

#### Пример 4: Уточнение данных

```
Вы: назначь встречу

Бот: На какое время назначить встречу?

Вы: на завтра в 15:00

Бот: [обрабатывает уточненный запрос...]
```

## Контракт с LLM

Бот ожидает от LLM строго структурированный JSON в следующем формате:

```json
{
  "intent": "create" | "list" | "delete" | "unknown",
  "confidence": 0.0-1.0,
  "slots": {
    "title": "название события",
    "start": "2025-11-27T15:00:00+03:00",
    "end": "2025-11-27T16:00:00+03:00",
    "date": "2025-11-27",
    "participants": ["имя1", "имя2"],
    "description": "описание",
    "location": "место",
    "event_id": null
  },
  "clarify": {
    "needed": false,
    "questions": []
  }
}
```

### Правила работы с LLM:

1. **Только JSON** - LLM должен возвращать только JSON, без дополнительного текста
2. **ISO8601 формат** - все даты/время в формате ISO8601 с часовым поясом +03:00 (Moscow)
3. **Уточнения** - если данных недостаточно, LLM возвращает `clarify.needed=true` и список вопросов
4. **Confidence** - уровень уверенности от 0.0 до 1.0

Подробные примеры промптов и ожидаемых ответов находятся в файле `prompts/prompt_templates.json`.

## Тестирование

Для ручного тестирования используйте примеры из файла `examples_tests.md`. Этот файл содержит:

- Тестовые фразы пользователя
- Ожидаемые JSON-ответы от LLM
- Ожидаемые действия Google Calendar
- Примеры ответов бота

### Рекомендуемый порядок тестирования:

1. **Создание события** - проверьте создание события с полными данными
2. **Просмотр событий** - убедитесь, что события отображаются корректно
3. **Удаление события** - проверьте удаление по названию и по ID
4. **Уточнения** - проверьте работу системы уточняющих вопросов
5. **Низкая уверенность** - проверьте запрос подтверждения при низком confidence

## Структура проекта

```
Bot_Test/
├── main.py                    # Точка входа, запуск бота
├── config.py                  # Конфигурация, загрузка .env
├── schema.py                  # Pydantic-схемы для валидации JSON
├── llm_client.py             # Клиент для работы с Open Router API
├── calendar_client.py        # Клиент для работы с Google Calendar API
├── handlers.py               # Обработчики сообщений Telegram
├── requirements.txt          # Зависимости Python
├── .env.example              # Пример переменных окружения
├── README.md                 # Этот файл
├── examples_tests.md         # Тестовые примеры
├── prompts/
│   └── prompt_templates.json # Шаблоны промптов для LLM
├── credentials.json          # Google OAuth credentials (не в репозитории!)
├── token.json                # Google OAuth token (не в репозитории!)
└── .env                      # Переменные окружения (не в репозитории!)
```

## Обработка ошибок

### Ошибка: "TELEGRAM_TOKEN не установлен"

**Решение:** Проверьте, что файл `.env` существует и содержит `TELEGRAM_TOKEN`.

### Ошибка: "credentials.json не найден"

**Решение:** 
1. Создайте OAuth 2.0 клиент в Google Cloud Console
2. Скачайте JSON файл с учетными данными
3. Переименуйте его в `credentials.json` и поместите в корень проекта

### Ошибка: "expired token" при работе с Google Calendar

**Решение:** 
1. Удалите файл `token.json`
2. Перезапустите бота
3. Пройдите авторизацию заново

### Ошибка: "rate limit" от Open Router

**Решение:** 
1. Проверьте лимиты вашего тарифа на Open Router
2. Подождите некоторое время перед повторной попыткой
3. Рассмотрите возможность обновления тарифа

### LLM возвращает невалидный JSON

**Решение:**
- Бот автоматически попытается извлечь JSON из ответа
- Если это не удастся, бот попросит пользователя переформулировать запрос
- Проверьте логи для деталей ошибки

## Логирование

Бот логирует все операции в stdout с уровнем INFO. Для отладки можно изменить уровень логирования в `.env`:

```env
LOG_LEVEL=DEBUG  # DEBUG, INFO, WARNING, ERROR
```

## Безопасность

⚠️ **Важно для учебного проекта:**

- Файлы `.env`, `credentials.json` и `token.json` содержат секретные данные
- **НЕ коммитьте** эти файлы в репозиторий
- Добавьте их в `.gitignore`:
  ```
  .env
  credentials.json
  token.json
  __pycache__/
  *.pyc
  venv/
  ```

## Ограничения

- Single-user бот (работает с одним Google аккаунтом)
- Локальный запуск (не развернут в облаке)
- Использует бесплатную модель Mistral через Open Router (могут быть лимиты)

## Дальнейшее развитие

Возможные улучшения:

- [ ] Поддержка редактирования событий (edit/postpone)
- [ ] Inline-кнопки для выбора даты/времени
- [ ] Поддержка повторяющихся событий
- [ ] Уведомления о предстоящих событиях
- [ ] Поддержка нескольких календарей
- [ ] Мультиязычность

## Лицензия

Учебный проект.

## Контакты

Для вопросов и предложений создайте issue в репозитории.

